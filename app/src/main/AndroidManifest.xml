<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.darryl.assignment2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DifficultySelection" />
        <activity android:name=".EasyLevelSelection" />
        <activity android:windowSoftInputMode="stateHidden|adjustResize" android:name=".EasyLevel" />
        <activity android:name=".MediumLevelSelection" />
        <activity android:windowSoftInputMode="stateHidden|adjustResize" android:name=".MediumLevel" />
        <activity android:name=".HardLevelSelection" />
        <activity android:windowSoftInputMode="stateHidden|adjustResize" android:name=".HardLevel" />
    </application>

</manifest>